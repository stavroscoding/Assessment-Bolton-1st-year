public class Array_Sum {
        public static void main(String[] args) {
            // We create and initialize the array of integers.
            //    You can change these values or add/remove elements as needed.
            int[] numbers = { 10, 20, 30, 40, 50,60,70,80,90 };

            // We declare a variable to hold the running total.
            int sum = 0;

            // We Loop through each element in the array and add it to 'sum'.
            //    Using the enhanced for-loop for clarity.
            for (int number : numbers) {
                sum += number;  // same as sum = sum + number
            }

            //  Print out the final sum
            System.out.println("The sum of the array elements we created is: " + sum);
        }
    }
